import type { Meta, StoryObj } from '@storybook/react';
import DoneTagIcon from '../DoneTagIcon';

const meta: Meta = {
  title: 'DoneTagIcon',
  component: DoneTagIcon,
};

export default meta;

export type Story = StoryObj<typeof DoneTagIcon>;

export const Default: StoryObj = {};
