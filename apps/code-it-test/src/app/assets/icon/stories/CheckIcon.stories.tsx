import type { Meta, StoryObj } from '@storybook/react';
import CheckIcon from '../CheckIcon';

const meta: Meta = {
  title: 'CheckIcon',
  component: CheckIcon,
};

export default meta;

export type Story = StoryObj<typeof CheckIcon>;

export const Default = {};
