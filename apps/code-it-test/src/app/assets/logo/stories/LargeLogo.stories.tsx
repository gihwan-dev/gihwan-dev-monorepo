import type { Meta, StoryObj } from '@storybook/react';
import LargeLogo from '../LargeLogo';

const meta: Meta = {
  title: 'LargeLogo',
  component: LargeLogo,
};

export default meta;

export type Story = StoryObj<typeof LargeLogo>;

export const Default = {};
